# Notebooks

Optional space for exploratory notebooks.

The project is script-first so it remains simple to reproduce from the command line. If you want to add a notebook later, use the same logic from `src/`.
